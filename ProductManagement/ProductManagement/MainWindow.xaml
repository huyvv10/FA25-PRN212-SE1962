<Window x:Class="ProductManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProductManagement"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" WindowStartupLocation="CenterScreen" Background="#eee">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>            
        </Grid.RowDefinitions>
        <Border Background="Aqua" Padding="10" >
            <TextBlock Text="PRODUCT MANAGEMENT" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="10">
            <TextBox Padding="5" Width="600" HorizontalAlignment="Center" Margin="0,0,5,0"/>
            <Button Content="Search" Background="Yellow" Margin="15,0,0,0" Width="85"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="Add" Background="Yellow" />
            <Button Content="Update" Background="Yellow" />
            <Button Content="Delete" Background="Yellow" />
            <Button Content="Close" Background="Yellow" />            
        </StackPanel>
        <StackPanel Margin="0,10,0,0" Grid.Row="3">
            <DataGrid AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Width="*"/>
                    <DataGridTextColumn Header="Product name" Width="3*"/>
                    <DataGridTextColumn Header="Unit" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Width="*"/>
                    <DataGridTextColumn Header="Category" Width="2*"/>
                </DataGrid.Columns>                
                
            </DataGrid>
        </StackPanel>
        
    </Grid>
</Window>
